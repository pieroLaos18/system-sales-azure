config:
  target: "http://localhost:5000"
  phases:
    - duration: 1
      arrivalRate: 300
scenarios:
  - flow:
      - get:
          url: "/api/products/categoria/1" # Cambia el ID según tus categorías reales